<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.arise.com.dao.COMGM003Dao">
    <resultMap id="resultCOMGM003Dto" type="jp.arise.com.dto.COMGM003Dto">
        <result column="username" property="user"/>
        <result column="userid" property="userId"/>
    </resultMap>

    <select id="getUser" resultMap="resultCOMGM003Dto">
        SELECT username
        FROM test
        WHERE userid = #{userId}
    </select>

    <select id="getSyainDt" resultMap="resultCOMGM003Dto">
        SELECT username
        FROM test
        WHERE
        syainId = #{syainId} and
		seinengappiFrom = #{seinengappiFrom} and
		seinengappiTo = #{seinengappiTo} and
		previous = #{previous} and
		psition = #{psition} and
		nyusyabiFrom = #{nyusyabiFrom} and
		nyusyabiTo = #{nyusyabiTo} and
		teamNa = #{teamNa} and
		genbaNa = #{genbaNa} and
		phase = #{phase} and
		tanka = #{tanka} and
		moyori1Station = #{moyori1Station} and
		moyori2Station = #{moyori2Station} and
		moyori3Station = #{moyori3Station} and
		address = #{address}

    </select>

    <select id="getGenbaDt" resultMap="resultCOMGM003Dto">
        SELECT username
        FROM test
        WHERE
        genbaNa = #{genbaNa} and
		phase = #{phase} and
		moyori1Station = #{moyori1Station} and
		moyori2Station = #{moyori2Station} and
		moyori3Station = #{moyori3Station} and
		address = #{address}

    </select>

</mapper>